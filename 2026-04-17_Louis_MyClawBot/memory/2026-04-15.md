# 2026-04-15 Daily Memory

## LTX 2.3 VAE 文件损坏（今日主要进展）

### 问题
- `LTX2_video_vae_bf16.safetensors`：文件损坏。文件头结构正确（safetensors header 显示 flux2-vae 格式），但实际权重数据是乱码/被截断。ComfyUI 报错：`decoder.conv_in.weight` shape [512,512,3,3] 期望 9.4MB 但实际 latent 偏移只有 590KB。HF mirror 下载的文件本身就不完整。
- `LTX2_audio_vae_bf16.safetensors`：682KB，应该是 2.45GB。完全损坏。

### qBittorrent Torrent 状态
- ltx23.torrent 添加到 qBittorrent（PID 29168），但无 peer 连接
- 原因：私 torrent + 网络环境限制（无传入连接），无法通过 DHT 找到 peers
- torrent 内容：142.6 GB，26 files；正确的 VAE 文件在 torrent 里（flux2-vae.safetensors 0.22GB, LTX2_audio_vae_bf16.safetensors 2.45GB）

### 关键发现
- **flux2-vae.safetensors** 存在于 `Documents\ComfyUI\models\vae\flux2-vae.safetensors`（0.22GB），文件结构正确（有 `post_quant_conv.weight`），可以正常被 VAELoaderKJ 加载
- **VAELoaderKJ** 的 audio VAE 判断逻辑：`"vocoder.conv_post.weight" in sd or "vocoder.vocoder.conv_post.weight" in sd` → 认为是 AudioVAE；否则用标准 VAE
- ComfyUI-LTXVideo 的 `LTXVSpatioTemporalTiledVAEDecode` 只接受标准 VAE，不接受 AudioVAE
- **flux2-vae 不能替代 LTX2_video_vae**：两者 latent format 不同（8通道 vs 可能不同的配置）

### Shot 4 对比视频
- Wan 2.2：`cmp4_wan22.mp4` → `story/robin/cmp4_wan22.mp4`（2.24 MB, 1280×720, 121 frames, 8 steps, seed=42）
- HunyuanVideo：`cmp_hunyuan.mp4` → `story/robin/cmp_hunyuan.mp4`（0.96 MB）
- LTX：blocked（损坏的 VAE）

### 下一步
1. 等 qBittorrent torrent 找到 peers（可能需要几小时）
2. 同时可以开始用 Wan 2.2 + HunyuanVideo 做 shot 4 对比
3. 或者开始批量渲染所有 19 shots（用 Wan 2.2）
---

## 下午补充（14:00-16:00）

### Wan 2.2 re-run 完成
- 简化 prompt（去掉 camera 指令）成功渲染 shot 4
- **关键修复**：ComfyUI workflow 必须有 `SaveImage` 节点才能提交成功（之前报 "Prompt has no outputs"）
- 121 帧生成 → `C:\Users\user.V915-31\Documents\ComfyUI\output\cmp4_wan22_v2_*.png`（160MB）
- 用 Python imageio-ffmpeg 合并为 MP4：`cmp4_wan22_simple.mp4`（1.76MB）
- 存为 `story/robin/cmp4_wan22_simple.mp4`

### Shot 4 对比文件（全部）
| 模型 | 文件 | 大小 | 状态 |
|------|------|------|------|
| Wan 2.2（简化prompt）| cmp4_wan22_simple.mp4 | 1.76MB | ✅ 完成 |
| Wan 2.2（原始prompt）| cmp4_wan22.mp4 | 2.24MB | ✅ 完成 |
| HunyuanVideo | cmp_hunyuan.mp4 | 0.96MB | ✅ 完成 |
| LTX 2.3 | — | — | ❌ blocked（VAE损坏） |

### HunyuanVideo pipeline 仍未解决
- `easy hunyuanDiTLoader` 无法使用（模型不在 checkpoints/ 目录）
- `CLIPLoader(type='sd3')` + `CLIPTextEncode` 可以加载 CLIP 但不确定 conditioning 格式是否正确
- 下一步：直接用 `CLIPLoader(type='wan')` 替代 Hunyuan 的 CLIP，通过实验验证输出

### ComfyUI 必须有输出节点
- 之前 400 错误 "Prompt has no outputs"：因为没有 SaveImage/SaveVideo 等输出节点
- 添加 `SaveImage` 后提交成功

### qBittorrent 仍无连接
- connection refused on 127.0.0.1:8080，qBittorrent WebUI 无响应
- torrent 无 peers

### 后续计划
1. 继续调试 HunyuanVideo 或切换其他方案
2. 等 qBittorrent torrent 有 peers 后测试 LTX 2.3
3. 三模型 shot 4 对比（目前只有 Wan 2.2 + HunyuanVideo，缺 LTX）


---

## 晚间补充（16:00-18:13）

### LTX 2.3 I2V Workflow 分析
- Louis 想用 `LTX-2.3_T2V_I2V_Single_Stage_Distilled_Full.json` workflow（位于 `ComfyUI-LTXVideo\example_workflows\2.3\`）
- 该 workflow 需要 22b 模型版本，和你已有的 19b 不兼容
- **需要的模型（来自 ltx23.torrent）：**
  - `checkpoints/ltx-2.3-22b-dev-nvfp4.safetensors`（20.21 GB）
  - `checkpoints/ltx-2.3-22b-distilled_transformer_only_fp8_scaled.safetensors`（21.86 GB）
  - `loras/ltx-2.3-22b-distilled-lora-384.safetensors`（7.08 GB）← 你缺这个
  - `text_encoders/gemma_3_12B_it_fp4_mixed.safetensors`（8.80 GB）← 你缺这个
  - `vae/flux2-vae.safetensors`（0.31 GB）
  - `vae/LTX2_audio_vae_bf16.safetensors`（0.20 GB）
  - `vae/LTX2_video_vae_bf16.safetensors`（2.28 GB）

### ltx23.torrent 详情
- **来源**：https://github.com/venetanji/ltx2-comfy-v915（模型通过 BitTorrent 分发）
- torrent 大小：132GB+，无公开 tracker，纯 DHT PEX
- **qBittorrent 问题**：WebUI 端口是 8889（不是默认 8080），DHT 能否找到 peers 取决于网络环境
- Louis 需要在 qBittorrent 开启 DHT + Peer Exchange

### qBittorrent WebUI
- 端口：8889（不是 8080）
- 状态：running（PID 14524）但 WebUI 可能未启用
- 配置文件：`C:\Users\user.V915-31\AppData\Roaming\qBittorrent\qBittorrent.ini`

### 待办
- [ ] 等 qBittorrent torrent 下载完成
- [ ] 确认 DHT + PEX 开启
- [ ] Shot 4 三模型对比质量评估（Wan ✅, Hunyuan ✅, LTX ❌）
- [ ] 开始批量渲染所有 19 shots（建议用 Wan 2.2）
