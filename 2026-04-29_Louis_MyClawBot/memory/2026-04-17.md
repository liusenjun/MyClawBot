# Daily Memory — 2026-04-17

## Secret Scanner 集成到 bot-memory-update skill

- Louis 要求给 bot-memory-update skill 增加安全检测，防止敏感信息（API key、token 等）上传到公共 GitHub 仓库
- 已编写 `skills/bot-memory-update/scan_secrets.py` — 扫描文件中的敏感信息，支持 --fix 生成脱敏副本
- **检测模式**：
  - HIGH: GitHub PAT (ghp_*/github_pat_*)、OpenAI API Key (sk-*)、Anthropic (sk-ant-*)、AWS Key (AKIA*)、Bearer Token、Database Connection String、Private Key Header、Discord Token 等
  - MEDIUM: Named Secret 字段 (api_key=/secret=/token=)
- **扫描范围**：.py/.js/.json/.yaml 等文本文件，跳过 .git/__pycache__/node_modules 等
- `scan_secrets.py` 和 `SKILL.md` 自身已加入 SKIP_FILES 防止自扫误报

## 发现真实敏感信息

| 文件 | Token | 状态 |
|------|-------|------|
| `download_skills.py` | `ghp_fZ05OYGM********POcNPa1XnvTf` | ✅ 已删除文件 |
| `openclaw.json` | Discord Bot + Account Token | 位于 `.openclaw/` 外，不会被上传 |
| `memory/2026-03-26.md` | `ghp_O2nENU...`（部分截断） | Louis 选择不清理 |

## SKILL.md 流程改动

1. **扫描时机调整**：扫描从 Step 0 移到 Step 6（备份文件夹复制完成后、上传前），只扫即将上传的内容
2. **Step 6**：扫描备份文件夹（/tmp/bot-repo/<备份文件夹>）
3. **Step 7**：上传（scan pass 才继续）
4. **Step 9**：上传成功后发报告（含 Repo URL）；检测到敏感信息立即中断告知用户

## 仓库安全检查结果

- 扫描了 MyClawBot 仓库最新 7 个备份
- `ghp_` 匹配项仅存在于 `memory/2026-03-26.md`（Louis 选择不处理）
- 其他匹配项（`hf_token`/`Bearer {hf_token}`）均为代码中的变量名，非真实 token

## 临时文件清理

- `check_secrets_in_repo.py` — 已删除
- `check_secrets_in_repo.ps1` — 已删除
- `download_skills.py` — 已删除

## 当前状态

- bot-memory-update skill 改动完成，扫描逻辑正确
- workspace 扫描 PASS，无敏感信息
- 待 Louis 决定是否撤销 GitHub PAT（Louis 表示不需要）
