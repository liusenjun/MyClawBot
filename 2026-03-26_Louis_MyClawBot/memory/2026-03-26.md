# 2026-03-26 Daily Log

## Session

### 记忆恢复
- Louis 分享了 GitHub 仓库 https://github.com/liusenjun/MyClawBot
- 这个仓库是 OpenClaw 记忆同步仓库
- 从中恢复了之前的记忆：MEMORY.md, USER.md, SOUL.md, TOOLS.md, memory/2026-03-25.md
- BOOTSTRAP.md 已删除
- Git 在当前环境不可用，Louis 建议我自己装

### Git 安装
- 用 winget 安装了 Git.Git v2.53.0
- 配置了 global user.email 和 user.name
- Commit 成功：10 files changed, 457 insertions

### Louis 确认
- Louis 确认现在记得之前认识
- 记忆已更新到 2026-03-26

### 补全同步（Louis 指出）
- Louis 说我只下载了根目录文件，memory/ 和 skills/ 没完整同步
- 补全了 skills/skill-vetter/（SKILL.md, _meta.json, .clawhub/origin.json）
- 补全了 skills/bot-memory-update/（SKILL.md）
- 现在全部从 MyClawBot 仓库同步完毕

### bot-memory-update skill 触发（第一次错误）
- 执行 skill → gh 未认证，无法使用
- 安装 gh → 仍未认证
- Louis 提供 GitHub PAT（ghp_O2nENU...）
- 把 token 暴露在 git remote URL 里（安全错误！）
- 后来 Louis 让我撤回重新触发 skill

### bot-memory-update skill 触发（第二次，正确执行）
- Louis 明确要求完整执行 skill，不只是手动 push
- 先撤回 token（从 remote URL 移除）
- 用 GH_TOKEN 环境变量完成 gh 认证
- 按 skill 流程：检查 repo → 创建备份文件夹 2026-03-26_Louis_MyClawBot/
- 复制全部 workspace 文件到备份文件夹
- git clone → add → commit → push 到 MyClawBot
- ✅ 备份完成，14 files pushed
- 注意：credential helper 在当前环境不生效，gh 用 GH_TOKEN env var 认证

### bot-memory-update skill review
- Louis 发现备份不符合预期（日期文件夹结构不对）
- 帮 Louis review skill，发现 Step 7 有严重 bug：
  - 用 `git init` 创建全新 repo，force push 覆盖了整个远程仓库
  - 导致 2026-03-25_Louis/ 历史备份被覆盖丢失
- 修正了 skill 逻辑：改用 `git clone` 保留历史，删除 `git init` 和 `--force`
- 重新执行一次正确备份

### Discord Gateway 掉线问题排查
- Louis 反映最近 Gateway 经常掉线
- 查看日志发现：双 node 进程同时运行（昨天和今天的）
- 旧进程 24844 还在跑，新进程 5464 也启动了，互相冲突
- 这是频繁断线的直接原因
- 手动杀掉了旧进程 24844
- 在 openclaw.json 里给 Discord channel 加了 `"healthMonitor": {"enabled": false}`
- 禁用后 health-monitor 不会再因 stale-socket 尝试重启导致崩溃

### ClawHub skill 调研
- Louis 想找上传 openclaw 文件快照的 skill
- 在 ClawHub 上找到 `openclaw-backup`（alex3alex，16k 下载，58 stars）
- 和 Louis 详细讲解了区别：
  - openclaw-backup：tar.gz 压缩包备份整个 ~/.openclaw/，本地轮转保留7个
  - bot-memory-update：git 文件夹快照备份 workspace 到 GitHub，永久历史
- 两个可以共存互补

### 今天其他事项
- gateway 进程管理问题：`openclaw gateway restart` 不会正确杀掉旧进程
- 记录：以后重启前先查进程 ID，确保杀掉旧进程再启动新的
